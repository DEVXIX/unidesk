PySide6-Essentials>=6.11
ruamel.yaml>=0.18
materialyoucolor>=2.0
psutil>=6.0
pylnk3>=0.4
comtypes>=1.4
truststore>=0.10
fonttools>=4.50
pycaw>=20240316
# xD voice calls: a real LiveKit client, the audio devices, and the ring.
livekit>=1.0
sounddevice>=0.4
numpy>=1.26
# talking to the game window's browser, to hand it the xD session
websocket-client>=1.7
# and what a camera is captured with
opencv-python-headless>=4.10
